import { TouchableOpacity } from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { COLORS } from "../../../constants";

export default function MyIcon({name, color = COLORS.gray, activeColor = null}){
    return (
        <TouchableOpacity>
            
        </TouchableOpacity>
    )
}